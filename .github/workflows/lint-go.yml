name: Lint - Go
on:
  push:
    branches:
      - main
  pull_request:
  merge_group:
permissions:
  contents: read
  pull-requests: read
jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      - uses: actions/setup-go@0aaccfd150d50ccaeb58ebd88d36e91967a5f35b # v5
        with:
          go-version-file: "go.mod"
      - name: golangci-lint
        uses: golangci/golangci-lint-action@55c2c1448f86e01eaae002a5a3a9624417608d84 # v6.5.2
        with:
          args: --timeout=5m
          version: latest
      # - name: Install differ
      #   run: go install github.com/kevinburke/differ@1.2
      # - name: tidy
      #   run: differ go mod tidy
      # - name: Generate
      #   run: |
      #     go generate ./...
      #     git checkout -- go.sum
      #     differ true
      # - name: DB Migrations
      #   run: |
      #     go run -mod=mod db/create_migrations.go lint
      #     differ true
